
import JhiFormatter from '@/shared/config/Formatter';

describe('Formatter component', () => {
  let formatter = new JhiFormatter(); 

  beforeEach(() => {

  });

  it('should be an Object', () => {
    //GIVEN
    
    
    //WHEN

    //THEN
    expect(isObject(formatter).called).toBeTruthy();

  });



  // it('should remove user activity', () => {
  //   // GIVEN
  //   <%=jhiPrefix%>Tracker.activities = [{ page: 'login', sessionId: '123' }];

  //   // WHEN
  //   listenerObserver.next({ page: 'logout', sessionId: '123' });

  //   // THEN
  //   expect(<%=jhiPrefix%>Tracker.activities).toEqual([]);
  // });
});